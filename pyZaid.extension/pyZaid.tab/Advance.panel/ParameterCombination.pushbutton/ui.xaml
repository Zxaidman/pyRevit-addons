<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Parameter Combination - Advanced" Height="680" Width="1150" 
        WindowStartupLocation="CenterScreen" Background="#F5F5F5" FontFamily="Segoe UI" UseLayoutRounding="True">
    
    <Window.Resources>
        <!-- ============================================== -->
        <!-- THEME COLOR PALETTE                            -->
        <!-- ============================================== -->
        <Color x:Key="AccentColor">#3498DB</Color>
        <Color x:Key="SuccessColor">#27AE60</Color>
        <Color x:Key="DangerColor">#E74C3C</Color>
        <Color x:Key="LightBgColor">#F5F5F5</Color>
        <Color x:Key="DarkTextColor">#2C3E50</Color>
        <Color x:Key="GrayTextColor">#7F8C8D</Color>
        <Color x:Key="WhiteColor">#FFFFFF</Color>
        <Color x:Key="BorderColor">#BDC3C7</Color>
        <Color x:Key="DividerColor">#D5D8DC</Color>
        <Color x:Key="SoftHighlightColor">#EBF5FB</Color>

        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}" />
        <SolidColorBrush x:Key="DarkTextBrush" Color="{StaticResource DarkTextColor}" />
        <SolidColorBrush x:Key="GrayTextBrush" Color="{StaticResource GrayTextColor}" />
        <SolidColorBrush x:Key="WhiteBrush" Color="{StaticResource WhiteColor}" />
        <SolidColorBrush x:Key="LightBgBrush" Color="{StaticResource LightBgColor}" />
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
        <SolidColorBrush x:Key="DividerBrush" Color="{StaticResource DividerColor}" />
        <SolidColorBrush x:Key="SoftHighlightBrush" Color="{StaticResource SoftHighlightColor}" />

        <!-- ============================================== -->
        <!-- SHARED STYLES                                  -->
        <!-- ============================================== -->
        
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource WhiteBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="15" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="10" ShadowDepth="2" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style TargetType="Control" x:Key="InputStyle">
            <Setter Property="Background" Value="{StaticResource WhiteBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
        </Style>

        <!-- Buttons -->
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="15,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource WhiteBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Padding="15,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EBF5FB"/>
                    <Setter Property="Foreground" Value="#2980B9"/>
                    <Setter Property="BorderBrush" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Animated Toggle Switch -->
        <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="SwitchPill" Width="36" Height="20" CornerRadius="10" Background="{StaticResource BorderBrush}" VerticalAlignment="Center">
                                <Ellipse x:Name="SwitchThumb" Width="14" Height="14" Fill="White" Margin="3,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            </Border>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="SwitchPill" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="SwitchThumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ============================================== -->
        <!-- SAFE DATAGRID STYLES                           -->
        <!-- ============================================== -->
        
        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#E8E8E8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
        </Style>

        <Style x:Key="SafeRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="{StaticResource WhiteBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SoftHighlightBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SafeCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="4,0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SoftHighlightBrush}"/>
                    <Setter Property="Foreground" Value="{StaticResource DarkTextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PreviewTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
        </Style>

    </Window.Resources>

    <!-- ============================================== -->
    <!-- MAIN LAYOUT                                    -->
    <!-- ============================================== -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   
            <RowDefinition Height="*"/>      
            <RowDefinition Height="Auto"/>   
        </Grid.RowDefinitions>

        <!-- TOP HEADER BAR -->
        <Border Grid.Row="0" Background="{StaticResource DarkTextBrush}" Padding="15,10" Margin="15,15,15,10" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Parameter Combination - Advanced" FontSize="16" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1" Text="Batch Processor" FontSize="12" Foreground="{StaticResource BorderBrush}" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- BODY SECTION -->
        <Grid Grid.Row="1" Margin="15,0,15,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT PANEL -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="1. Target Parameter (to be modified)" Style="{StaticResource HeaderStyle}"/>
                    <ComboBox Grid.Row="1" x:Name="TargetParamCombo" Margin="0,0,0,20" Height="30"
                              IsEditable="True" IsTextSearchEnabled="False" StaysOpenOnEdit="True" Style="{StaticResource InputStyle}"/>

                    <TextBlock Grid.Row="2" Text="2. Write Formula or Text" Style="{StaticResource HeaderStyle}"/>
                    
                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
                        <CheckBox x:Name="ToggleEvalMath" Content="Force Math Eval" Style="{StaticResource ToggleSwitchStyle}" Margin="0,0,20,0" ToolTip="Evaluate math formulas instead of writing raw text."/>
                        <CheckBox x:Name="ToggleMixedMode" Content="Mixed Math + &quot;String&quot;" Style="{StaticResource ToggleSwitchStyle}" ToolTip="Calculate math outside double-quotes, keep text inside double-quotes."/>
                    </StackPanel>

                    <Grid Grid.Row="4" Margin="0,0,0,15">
                        <RichTextBox x:Name="FormulaRichTextBox" AcceptsReturn="True" 
                                     VerticalScrollBarVisibility="Auto" Style="{StaticResource InputStyle}" Padding="6">
                            <FlowDocument>
                                <Paragraph Margin="0"/>
                            </FlowDocument>
                        </RichTextBox>
                        <Popup x:Name="SuggestPopup" PlacementTarget="{Binding ElementName=FormulaRichTextBox}" 
                               Placement="Bottom" StaysOpen="False" MaxHeight="180" Width="280">
                            <Border BorderBrush="{StaticResource AccentBrush}" BorderThickness="1.5" CornerRadius="4">
                                <ListBox x:Name="SuggestListBox" Background="White" Foreground="{StaticResource DarkTextBrush}" BorderThickness="0"/>
                            </Border>
                        </Popup>
                    </Grid>

                    <Grid Grid.Row="5" VerticalAlignment="Bottom">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" x:Name="SourceParamCombo" Margin="0,0,10,0" Height="30"
                                  IsEditable="True" IsTextSearchEnabled="False" StaysOpenOnEdit="True" Style="{StaticResource InputStyle}"/>
                        <Button Grid.Column="1" x:Name="InsertParamBtn" Content="Insert" Width="80" Height="30" 
                                Background="#E8E8E8" Foreground="{StaticResource DarkTextBrush}" BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Grid>
            </Border>

            <!-- RIGHT PANEL -->
            <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="3. Real-Time Result Preview" Style="{StaticResource HeaderStyle}"/>
                    
                    <!-- Safely configured DataGrid -->
                    <DataGrid Grid.Row="1" x:Name="ElementGrid"
                              Background="{StaticResource WhiteBrush}"
                              BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                              AlternatingRowBackground="{StaticResource LightBgBrush}"
                              RowBackground="{StaticResource WhiteBrush}"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="{StaticResource DividerBrush}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              SelectionMode="Extended"
                              SelectionUnit="FullRow"
                              ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                              RowStyle="{StaticResource SafeRowStyle}"
                              CellStyle="{StaticResource SafeCellStyle}">
                        
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="✓" Binding="{Binding Apply}" Width="40">
                                <DataGridCheckBoxColumn.ElementStyle>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridCheckBoxColumn.ElementStyle>
                            </DataGridCheckBoxColumn>
                            <DataGridTextColumn Header="ID" Binding="{Binding ID}" IsReadOnly="True" Width="80"/>
                            <DataGridTextColumn Header="Family" Binding="{Binding Family}" IsReadOnly="True" Width="140"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" IsReadOnly="True" Width="140"/> 
                            
                            <!-- Safe styling link -->
                            <DataGridTextColumn Header="Preview Result" Binding="{Binding Preview}" IsReadOnly="True" Width="*" ElementStyle="{StaticResource PreviewTextStyle}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- BOTTOM FOOTER -->
        <Border Grid.Row="2" Background="#E8E8E8" Padding="15,10" Margin="15,0,15,15" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" x:Name="StatusBar" Text="Ready" Foreground="{StaticResource GrayTextBrush}" 
                           FontWeight="SemiBold" VerticalAlignment="Center" FontSize="12"/>
                           
                <Button Grid.Column="1" x:Name="ApplyBtn" Content="Apply Checked Elements" Width="180" Margin="0,0,15,0" Style="{StaticResource PrimaryBtn}"/>
                <Button Grid.Column="2" x:Name="CancelBtn" Content="Cancel" Width="100" Style="{StaticResource SecondaryBtn}"/>
            </Grid>
        </Border>
    </Grid>
</Window>